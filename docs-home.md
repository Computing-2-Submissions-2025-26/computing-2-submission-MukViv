# Chess Thieves API Documentation

This documentation describes the pure game module in `web-app/game-logic.js`.

Use the navigation to inspect the exported game constants and functions, such
as `create_new_game`, `move_thief`, `move_king`, `place_barrier`, and
`check_winner`.
